{
  "protocol": "treeland_output-manager_v1",
  "description": "Smoke test (needs wl_output hardware fixture)",
  "tests": [
    {
      "name": "smoke-set_primary_output",
      "description": "Verifies client can bind, send set_primary_output, and not crash. needs wl_output hardware fixture",
      "steps": [
        {
          "type": "request",
          "name": "set_primary_output",
          "args": [
            "HEADLESS-1"
          ]
        },
        {
          "type": "roundtrip"
        },
        {
          "type": "checkpoint"
        }
      ],
      "expected": {
        "events": []
      }
    }
  ]
}