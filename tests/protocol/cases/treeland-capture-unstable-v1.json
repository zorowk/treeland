{
  "protocol": "treeland_capture_v1",
  "xml_path": "/usr/share/treeland-protocols/treeland-capture-unstable-v1.xml",
  "description": "Smoke test (needs wl_surface + pipewire stream fixture)",
  "tests": [
    {
      "name": "smoke-start",
      "description": "Verifies client can bind, send start, and not crash. needs wl_surface + pipewire stream fixture",
      "steps": [
        {
          "type": "request",
          "name": "get_context",
          "args": []
        },
        {},
        {
          "type": "checkpoint"
        }
      ],
      "expected": {
        "events": []
      }
    }
  ]
}
