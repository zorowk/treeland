{
  "protocol": "treeland_shortcut-manager_v2",
  "xml_path": "/usr/share/treeland-protocols/treeland-shortcut-manager-v2.xml",
  "description": "Smoke test (needs wl_surface + wl_seat fixture)",
  "tests": [
    {
      "name": "smoke-acquire",
      "description": "Verifies client can bind, send acquire, and not crash. needs wl_surface + wl_seat fixture",
      "steps": [
        {
          "type": "request",
          "name": "acquire",
          "args": []
        },
        {},
        {
          "type": "checkpoint"
        }
      ],
      "expected": {
        "events": []
      }
    }
  ]
}
