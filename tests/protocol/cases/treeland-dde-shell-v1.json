{
  "protocol": "treeland_dde-shell_v1",
  "description": "Smoke test (needs wl_surface + window management fixture)",
  "tests": [
    {
      "name": "smoke-get_window_overlap_checker",
      "description": "Verifies client can bind, send get_window_overlap_checker, and not crash. needs wl_surface + window management fixture",
      "steps": [
        {
          "type": "request",
          "name": "get_window_overlap_checker",
          "args": []
        },
        {},
        {
          "type": "checkpoint"
        }
      ],
      "expected": {
        "events": []
      }
    }
  ],
  "xml_path": "/usr/share/treeland-protocols/treeland-dde-shell-v1.xml"
}
