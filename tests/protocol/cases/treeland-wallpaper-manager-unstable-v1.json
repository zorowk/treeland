{
  "protocol": "treeland_wallpaper-manager_v1",
  "xml_path": "/usr/share/treeland-protocols/treeland-wallpaper-manager-unstable-v1.xml",
  "description": "Smoke test (needs wl_output + wl_surface fixture)",
  "tests": [
    {
      "name": "smoke-get_treeland_wallpaper",
      "description": "Verifies client can bind, send get_treeland_wallpaper, and not crash. needs wl_output + wl_surface fixture",
      "steps": [
        {
          "type": "request",
          "name": "get_treeland_wallpaper",
          "args": []
        },
        {},
        {
          "type": "checkpoint"
        }
      ],
      "expected": {
        "events": []
      }
    }
  ]
}
